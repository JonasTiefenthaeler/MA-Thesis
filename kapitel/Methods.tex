\chapter{Methods}\label{chap: Methods}

TODO:
\begin{itemize}
    \item Write what surface parameters where used
    \item Write simuation setup
    \item Roughness und so für convergence study erklären
\end{itemize}

For this Thesis the high-order \acrfull{sem} code NEKO is used.
Neko is an open-source \acrfull{cfd} solver, that is based on the older Nek5000 \acrshort{cfd} solver done by \cite{fischerNek500WebPage} in the nineties.
The portable framework Neko is written in modern Fortran in an object-oriented manner and is mainly focused on incompresible flow simulations \citep{janssonNekoModernPortable2024}.
Only \acrfull{dns} will be performed in this work, however \acrfull{les} models are also implemented in NEKO.


\section{Simulation setup}\label{sec: Simulation setup}

\section{Generating rough surfaces}\label{sec:generating rough surfaces}

Surfaces in real-world engineering applications often exhibit irregular roughness.
In previous studies often scans of real-world surfaces are used, to perform the simulations, e.g. by \cite{thakkarInvestigationTurbulentFlow2017}, \cite{demaioDirectNumericalSimulation2023} and \cite{busseReynoldsnumberDependenceNearwall2017}.
This yields the advantage, that inherently the real world influence of surface roughness can be investigated.
This method is however very restricted by the number of surfaces and hence surface parameters, that can be tested.
Also, the investigated surface parameters can not be directly controlled, but are a direct result of the chosen surface to be scanned.
In the present work, the surface roughness will be generated by an algorithm introduced by \cite{perez-rafolsGeneratingRandomlyRough2019}.
In the following section a brief description of the algorithm is given.

\subsection{Surface generation algorithm}\label{subsec: Surface generation algorithm}

The idea behind the algorithm by~\cite{perez-rafolsGeneratingRandomlyRough2019} is, that a realistic surface topography needs a height distribution, as well as a spatial distribution.
To that extend, two main input parameters need to be specified.
To define the height distribution, a height probability distribution needs to be specified.
To define the spacial distribution, a power spectrum is used.
Using the probability distribution and the power spectrum, two initial surfaces are generated. 
The first surface $Z_h$ shows the desired probability distribution, but not necessarily the power spectrum.
The second surface $Z_s$ exhibits the power spectrum but not the probability distribution.
The algortighm iteratively corrects $Z_s$ and $Z_h$ to match each other, until a specified convergence is reached.
The iteration consits of two steps:
\begin{enumerate}
    \item Adjust the $Z_s$ using the formula $\hat{Z}_s^{i+1} = \hat{Z}_h^i \frac{|\hat{Z}_s^i|}{|\hat{Z}_h^i|}$ , with the values deonted by $\hat{Z}$ representing their respective fourier transforms.
          The inverse transform of $\hat{Z}_s^{i+1}$, $Z_s^{i+1}$, shows the specified power spectrum but a changed height probability distribution.
    \item Obtain a $Z_h^{i+1}$ by adjusting the height probability distribution of $Z_s^{i+1}$.
          This is acheived by rank ordering
\end{enumerate}
In this work, as the random distribution the Weibull distribution is used, with the probability density function
\begin{equation}\label{eq: Weibull distribution}
    f(z) = \frac{k}{\lambda} \left( \frac{z}{\lambda}\right)^{k-1} e^{-(z/k)^{k}}
\end{equation}
with $k$ being the scale factor and $\lambda$ representing the scale factor.
Z BESCHREIBEN.
The used power spectrum is defined by
\begin{equation}\label{eq: Power spectrum}
    C(q) = \frac{S_q^2 \beta}{2\pi\left(\beta^2 + q_x^2+q_y^2\right)^{\frac{3}{2}}}
\end{equation}
with $S_q$ being the rms height and $\frac{1}{\beta}$ is the autocorrelation length, as described by \cite{perez-rafolsGeneratingRandomlyRough2019}.
The specific random distribution and power spectrum are chosen, since they are thoroughly tested by \cite{perez-rafolsGeneratingRandomlyRough2019}.
It shows, that reasonably fast convergence, as well as low absolute errors can be acheived.
It is important to note, that the two surfaces are not exactly the same, but similar within the bounds of a defined tolerance.
$Z_h$ will have the desired height probability distribution, but the power spectrum will deviate slightly, within the capability of the algorithm.
Vice versa for $Z_s$.
It is chosen to use the surface $Z_h$ in this work.
The described method for generating rough surfaces needs several input parameters, namely $k, \lambda, S_q$ and $\beta$.
SCHREIBEN WIE ICH AUF DIE PARAMETER KOMME
NOCH SCHREIBEN DASS ES PERIODISCHE SURFACES GENERIERT

\subsection{Applying rough surfaces onto the mesh}\label{subsec: Generating rough mesh}

The algorithm described in section \ref{subsec: Surface generation algorithm} yields the height values for a plane surface.
This surface has to be mapped onto the pipe in such a way, that the same generated surface can be mapped onto different meshes with varying amounts of grid points.
This leads to the fact, that the straightforward method, i.e. applying the height values of the generated surface points directly onto the pipe mesh is not applied in this work.
The used approach relies on the fact, that every surface topography can be decomposed into a multitude of $\sin$ and $\cos$ waves.
The decomposed values are the Fourier coefficients, which can be seen as a wheight given for each mode.
By recombining the extracted modes, the surface can be recreated.
The Fourier coefficients are extracted by taking the real and imaginary part  of the Fourier transform of my generated rough surface, such that $A(m,n) = \operatorname{Re}\left(\hat{Z_h}\right)$ and $B(m,n) = \operatorname{Im}\left(\hat{Z_h}\right)$\\
This decomposition and recreation is only exact, if a infinite number of modes is used.
This is however only relevant for regular surfaces.
Since the surfaces generated by the algorithm described in section \ref{subsec: Surface generation algorithm} are defined with a finite amount of modes (that is chosen by the user), this amount of modes is sufficient for the recreation of the surface.
With these parameters, the height function for the rough surface at each individual point can be defined as
\begin{equation}\label{eq: height function}
    k(th,z) = \sum_{n,n}^{M,N} A(m,n)\cos(phase)- B(m,n)\sin(phase)
\end{equation}
with
\begin{equation}\label{eq: phase equation}
    phase = 2\pi\left(\frac{uz}{L_z} + \frac{vth}{L_{th}}\right)
\end{equation}
with $A(m,n)$ and $B(m,n)$ representing the Fourier coefficients, $m$ and $n$ the Fourier mode and $z, th$ the streamwise and azimuthal coordinate of the respective point.
The values $u$ and $v$ represent the shift.... HIER NOCH WAS SCHREIBEN.
This height function yields the recreation of the rough surface, which is applied on grid points at the wall.
However, a body fitted mesh is desired, which leads to the necessity, to create a scaling function for the grid points away from the wall.
The roughness function should be applied onto the mesh in the near wall region of the domain, while the inner region stays unaffected HIER VLT CHAN ZITIEREN.
The used function for this purpose is
\begin{equation}\label{eq: roughness scaling function}
    rs = R + \frac{R - r_{crit}}{r_{max} - r_{crit}} * k(th,z) 
\end{equation}
with $ 0 < r_{crit} < 1$ representing the minimal radius, at which the grid points are shifted, $r_{max}$ the maximum radius of the smooth pipe mesh and $R$ the radius of the point to be shifted
This function scales the magnitude of shift applied to each grid point linearly between the outer pipe radius of the original smooth pipe and $r_{crit}$, from one to zero.\\
The shift applied to the points is only in radial direction.
Since the GLL points are distributed onto the mesh before the roughness is applied, the GLL distribution is not exact after the rough deformation.
Since the shift is of small magnitude, the effect is assumed to be small.
A test on the influence of simulation results is beyond the scope of this work. 
NOCH AUF ANZAHL DER MODES EINGEHEN --> BEZUG ZU GRIDPOINTS?\\
GIBTS NE QUELLE FOR DIE HEIGHT FUNCTION?\\
WIE KOMME ICH AUF DIE FOURIER COEFFICIENTS?\\
AUF BODY FITTED VS. IBM EINGEHEN

\subsection{Finding good input parameters}\label{subsec: Finding input parameters}



\section{Postprocessing}\label{sec: Postprocessing}

In order to be able to work with the obtained data by the \acrshort{dns}, the data has to be postprocessed.
In this work, this is mainly done by using PySEMTools, which is a open source Python package, to postprocess \acrshort{sem} data on hexahedra high-order elements \cite{perezPySEMToolsLibraryPostprocessing2026}.\\
It is possible to obtain fluid statistics from multiply instananeous fields.
However, Neko has the capability to produce statistics files, in which the data is averaged in time and in a spacial direction of choice. 
No spacial averaging was chosen to be performed by Neko, so the statistics files contain 3D time averaged data.
In order to be able to generate first- and second order statstics of the turbulent fiels, this data has to be further processed.
The typical workflow is as follows:
\begin{itemize}
    \item In case multiple files exist that contain statistics data, average them to obtain a single time averaged field
    \item Interpolate the averaged data from the \acrshort{sem} mesh to user defined interpolation points
    \item Calculate the desired values based on the interpolated data
    \item Further average the 3D data, such that 2D or 1D plots can be generated
\end{itemize}
To be able to process data of rough cases properly, various modifications had to be made to the process, compared to a smooth case.

\subsection{Postprocessing rough cases}\label{subsec: Postprocessing rough cases}

The Postprocessing of rough \acrshort{dns} data is not as straightforward as for smooth cases.
Two of the several conceivable workflows will be described in this section.

\subsubsection{Mapping Interpolation points to match the rough surface}\label{subsubsec: Mapping interpolation points}
In this approach, the interpolation points are mapped to match the rough surface of the \acrshort{dns}.
Due to the matching field, the interpolation points can be interpolated onto the \acrshort{dns} field with all points being found by the interpolator.
The main issue with this approach is the fact, that the interpolation points, which contain the data that is used in the further process, are not uniformly distributed in the radial direction.
This will lead to the situation, that data points that are interpolated at different radii will be treated as if they were at a comparable position.

\subsubsection{Inverse mapping \acrshort{dns} data and interpolation points}

The second approach is to introduce a computational domain for the data interpolation.
To map the \acrshort{dns} data into the computational domain it is mapped back to a smooth pipe.
This is achieved by recalculating the height function of the rough surface using equation \ref{eq: height function} and equation \ref{eq: phase equation}.
The height function is  applied onto the rough mesh using the inverse of the roughness scaling function, equation \ref{eq: roughness scaling function}.
The inverse of the rough surface function can be expressed as
\begin{equation}
     surf_{inverse} = \frac{1.0}{(1.0 + \frac{surf_{change}}{pipe_R - r_{crit}})} * (mesh_{change} + \frac{r_{crit}}{pipe_R - r_{crit}} * surf_{change})
\end{equation}

\begin{figure}
    \centering
    \begin{subfigure}{0.45\textwidth}
        \includegraphics[width = \textwidth]{averaged_field.png}
        \subcaption{Averaged field of the velocity magnitude of an exemplary case}
        \label{subfig: averaged field}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.45\textwidth}
        \includegraphics[width = \textwidth]{smooth_Averaged_field.png}
        \subcaption{Averaged and smoothed field of the velocity magnitude of an exemplary case}
        \label{subfig: smooth averaged field}
    \end{subfigure}
    \caption{Comparison of averaged velocity magnitudes of rough field against rough pipe with smoothing algorithm applied}
    \label{fig: comparison of smooth and normal averaged field}
\end{figure}
The results are shown from an exemplary case in figure \ref{fig: comparison of smooth and normal averaged field}.
It can be observed, that the rough pipe is mapped back to the original radius of $R = 1$.\\
The data should be interpolated on concentric circles. 
If the smooth averaged field would be interpolated onto the smooth interpolation points, the generated data would be in a body fitted manner.
To acheive data extraction in a concentric way, the interpolation points have to be deformed as well. 
By applying the same formulation onto the interpolation points, as done for the rough pipe, the interpolation points are defored in such a way, that they lie in the computational domain in the same positions, as they would be in the physical domain for a concentric data extraction.

NOCH BILD MACHEN UM DAS ZU VERDEUTLICHEN


\section{Convergence study}

A grid resolution as well as time convergence was conducted, to find reasonable parameters for both, that yield sufficient results while keeping the necessary computational resources manageable.
The study was conducted using $Re_b = 5700$ in order to have faster computational times.
For all three cases the length of the pipe is set at $L_z = 8\pi R$, while the radius is unity $R = 1$. 

\subsection{Grid refinement study}\label{subsec: grid refinement study}

For the grid refinement study, three meshes of varying resolutions are used.
The resolution are altered in streamwise and azimuthal direction, while keeping the wall-normal resolution fixed.
Also, the polynomial order is kept constant at $p = 7$ ZITAT.
The used parameters are listed in \autoref{tab: parameters grid refinement study}
\begin{table}
\centering
\caption{Simulation parameters for the grid convergence study. $N_z, N_\theta, \Delta_z$ and $\Delta_\theta$ denoting the streamwise and azimuthal number of gridpoints or gridpoints spacings respectively. The wall normal resolution is kept constant for all cases}
\begin{tabular}{ c c c c c c c c } 
 \hline
  $Re_b$    & $Re_\tau$     & $N_z$     & $N_{\theta}$  & $\Delta_z$    & $\Delta_\theta$   & N elements & $Tu_\tau / R$\\
 \hline
 $5700$     & $246.07$      & $448$     & $224$         & $13.80$       & $6.90$            & $26752$    & $21.5$\\ 
 $5700$     & $246.00$      & $560$     & $290$         & $11.04$       & $5.33$            & $44000$    & $21.6$\\ 
 $5700$     & $246.21$      & $672$     & $336$         & $9.21$        & $4.60$            & $67392$    & $21.6$\\ 
 \hline
\end{tabular}
\label{tab: parameters grid refinement study}
\end{table}
To obtain statistics, the initial transient period is set to be $T = 500$ time units.
It is chosen to be quite generous, to midigate influences from transition to turbulence.
The initial transient phase is tested in the time convergence study in \autoref{subsec: Time convergence study}.
The statistics were taken for $250$ time units, hence between $500 - 750$ time units.
The results of the grid refinement study is shown in \autoref{fig: Mesh refinement study}.
In \autoref{subfig: grid convergence uplus} the mean streamwise velocitiy profiles are compared.
\autoref{subfig: grid convergence RS} shows the Reynolds stresses, while \autoref{subfig: grid convergence total stress} compares the total stresses, including their individual components viscous stress, Reynolds stress and dispersive stress.
\begin{figure}
    \centering
    \begin{subfigure}[t]{0.495\textwidth}
        \centering
        \subcaption{}
        \input{plots/grid_convergence_uplus.pgf}
        \label{subfig: grid convergence uplus}
        
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.495\textwidth}
        \centering
        \subcaption{}
        \input{plots/grid_convergence_RS.pgf}
        \label{subfig: grid convergence RS}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.495\textwidth}
        \centering
        \subcaption{}
        \input{plots/grid_convergence_totStress.pgf}
        \label{subfig: grid convergence total stress}
    \end{subfigure}
    \caption{First and second order statistics for mesh convergence study.~(a) Mean streamwise velocity profiles $U+$ over wall normal $y+$.~(b) Reynolds stresses $R_{ij}^+$ over wall normal distance $y^+$. Solid line representing $R_{uu}^+$, dashed line $R_{vv}^+$, dotted line $R_{ww}^+$ and dot-dashed line $R_{uv}^+$.~(c) Stresses over wall normal distance r/R. Solid line representing the total stress $\tau_{tot}$, the dashed line the Reynolds stress $\tau_{uv}$, dotted line the viscous stress $\tau_{vis}$ and the dot-dashed line the dispersive stress $\tau_{disp}$. Red line chosen for further study.}
    \label{fig: Mesh refinement study}
\end{figure}
The mean streamwise velocity profiles in \autoref{subfig: grid convergence uplus} show nearly no influence of the grid resolution, which indicates little affect of grid resolution on first order statistics, within reasonable bounds ZITAT.
The Reynolds stresses in \autoref{subfig: grid convergence RS} show partially a small dependence on grid resolution.
The values for $R_{vv}, R_{ww}$ and $R_{uv}$ are almost unaffected.
However, $R_{uu}$ shows a steady increase in its maximum value between $4.06$ and $4.17$ for increasing finer meshes.
This results in a difference of $~ 2.6\%$ between the highest and lowest value.
The total stress in \autoref{subfig: grid convergence total stress} seems to be nearly unaffected by the grid resolution. 
All meshes, including the coarsest mesh shows the expected slope of $1$ in the center region of the pipe ZITAT.
Additionally, the trends for the viscous stress are identical.
However, there are slightly different trends for the Reynolds stress, as already described above, and the dispersive stress.
The dispersive stress decreases with increasing mesh resolution.
The finest mesh shows the lowest dispersive stress, especially in the transition and log-law region. 
The curves collapse onto another again near the center, while reaching zero.
Also in the near-wall region, they behave similarly.\\
From the grid convergence study can be concluded, that influence on the shown statiics is small for the used meshes.
However, it is expected, that the differences will grow with higher Reynolds numbers ZITAT.
For that reason, the coarsest mesh is not chosen for the study. 
The number of elements between the finest mesh, and the intermediate mesh is $~64\%$ higher and thus a substantial increase in computational time is expected.
Since the goal of this study is to compare the results among each other, and not necessarily compare to reference data, and a sufficient convergence is shown, the chosen mesh is the one depicted in red in \autoref{fig: Mesh refinement study}.

\subsection{Time convergence study}\label{subsec: Time convergence study}

Using the chosen mesh from \autoref{subsec: grid refinement study}, the time convergence is tested.
The time convergence is tested, to find the necessary initial transition time to turbulence, at which the computation of averaged values can start.
The simulated time interval is from $300-750$ time units.
Every $100$ time units, averaged files were writte, such the flow behaviour can be studied in time intervals of $100$ time units.
The studied averaging times are $500-750, 300-750, 300-600$ and $300-500$.
The results of the time convergence study are shown in \autoref{fig: Time convergence study}.
\begin{figure}
    \centering
    \begin{subfigure}[t]{0.495\textwidth}
        \centering
        \subcaption{}
        \input{plots/time_convergence_uplus.pgf}
        \label{subfig: time convergence uplus}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.495\textwidth}
        \centering
        \subcaption{}
        \input{plots/time_convergence_RS.pgf}
        \label{subfig: time convergence RS}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.495\textwidth}
        \centering
        \subcaption{}
        \input{plots/time_convergence_totStress.pgf}
        \label{subfig: time convergence total stress}
    \end{subfigure}
    \caption{First and second order statistics for time convergence study.~(a) Mean streamwise velocity profiles $U+$ over wall normal $y+$.~(b) Reynolds stresses $R_{ij}^+$ over wall normal distance $y^+$. Solid line representing $R_{uu}^+$, dashed line $R_{vv}^+$, dotted line $R_{ww}^+$ and dot-dashed line $R_{uv}^+$.~(c) Stresses over wall normal distance r/R. Solid line representing the total stress $\tau_{tot}$, the dashed line the Reynolds stress $\tau_{uv}$, dotted line the viscous stress $\tau_{vis}$ and the dot-dashed line the dispersive stress $\tau_{disp}$. Black line chosen for further study.}
    \label{fig: Time convergence study}
\end{figure}
\autoref{subfig: time convergence uplus} shows the average streamwise velocity profiles, \autoref{subfig: time convergence RS} shows the foure Reynolds stresses and \autoref{subfig: time convergence total stress} the total stress, as well as its individual components, as done in \autoref{subsec: grid refinement study}.
It becomes clear, that the influence of the averaging times on the statistics is very little.
Only minor differences are existent and if they are, they are minor.
This indicates validity of the chosen averaging time in \autoref{subsec: grid refinement study}.
Furthermore, it can be seen, that averaging until $750$ time units is not necessary to obtain valid statistics.
Eventhough the results shown here support the assumption, that an averaging time from $300-500$ time units is sufficient, it is not chosen for the further study.
The following study is conducted using averaging between $300-600$ time units, since the time convergence study is performed using $Re_b = 5700$ and higher bulk Reynolds number will be used in the future.
To that extent to decrease the uncertainty a longer averaging time is chosen.